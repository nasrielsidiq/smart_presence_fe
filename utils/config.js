const API_BASE_URL = 'https://5291-36-93-6-234.ngrok-free.app/api';

export const ENDPOINTS = {
    USERS: `${API_BASE_URL}/users`,
    LOGIN: `${API_BASE_URL}/login`,
    
    // Tambahkan endpoint lain di sini jika diperlukan
};